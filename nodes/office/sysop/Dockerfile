FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Expose port 8080 (Internal)
EXPOSE 8080

# Run uvicorn on port 8080
CMD ["uvicorn", "scribe:app", "--host", "0.0.0.0", "--port", "8080"]
